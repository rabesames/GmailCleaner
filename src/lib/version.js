// __APP_VERSION__ is a build-time global (see vite.config.js's `define`),
// substituted with package.json's `version` -- the single source of truth
// for the app's semver number, bumped by hand there on release.
export const APP_VERSION = __APP_VERSION__;
