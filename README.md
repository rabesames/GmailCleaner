# GmailCleaner
A single page React app that allows a user to summarize all gmail messages grouped according to sender and shows the total size and count of emails from that sender. This then enables the user to clean senders that take up a lot of space with one button click.
